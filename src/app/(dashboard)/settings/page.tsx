import { PlaceholderContent } from "@/components/layout/placeholder-content";
import { Settings } from "lucide-react";

export default function SettingsPage() {
    return <PlaceholderContent title="Settings" description="A module to manage application-wide settings, user preferences, and access controls." icon={Settings} />
}
