import { PlaceholderContent } from "@/components/layout/placeholder-content";
import { ShoppingCart } from "lucide-react";

export default function OrdersPage() {
    return <PlaceholderContent title="Order Management" description="Manage the full lifecycle of orders from creation to fulfillment." icon={ShoppingCart} />
}
