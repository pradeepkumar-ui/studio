import { PlaceholderContent } from "@/components/layout/placeholder-content";
import { FileJson } from "lucide-react";

export default function OfferConstructionSettingsPage() {
    return <PlaceholderContent 
        title="Offer Construction Settings" 
        description="A module to manage templates, rules, and data sources for offer construction." 
        icon={FileJson} 
    />
}
