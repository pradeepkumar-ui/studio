import { PlaceholderContent } from "@/components/layout/placeholder-content";
import { Plane } from "lucide-react";

export default function InventoryPage() {
    return <PlaceholderContent title="Flight & Inventory" description="Manage booking classes, and map fare brands to booking classes/fare bases." icon={Plane} />
}
