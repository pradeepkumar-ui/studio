import { PlaceholderContent } from "@/components/layout/placeholder-content";
import { FileText } from "lucide-react";

export default function DocumentationPage() {
    return <PlaceholderContent 
        title="Order Documentation" 
        description="A module for the generation, storage, versioning, and dissemination of all documents associated with the lifecycle of an airline Order." 
        icon={FileText} 
    />
}
