import { PlaceholderContent } from "@/components/layout/placeholder-content";
import { Layers } from "lucide-react";

export default function OfferComposerPage() {
    return <PlaceholderContent title="Offer Composer" description="Compose NDC-compatible offers by discovering and pricing solutions and ancillaries." icon={Layers} />
}
