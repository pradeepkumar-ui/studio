import { PlaceholderContent } from "@/components/layout/placeholder-content";
import { BookOpen } from "lucide-react";

export default function CatalogPage() {
    return <PlaceholderContent title="Catalogue" description="Define airline products with attributes, validity, and version control." icon={BookOpen} />
}
