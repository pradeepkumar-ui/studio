
services:
  offersensev2:
    build: .
    container_name: offersensev2
    restart: always
    ports:
      - "8006:8006"
    env_file:
      - .env.production
    environment:
      - NODE_ENV=production
    networks:
      - offersense-network

networks:
  offersense-network:
    driver: bridge